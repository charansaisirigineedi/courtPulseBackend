spring:
  datasource:
    url: jdbc:h2:mem:courtpulse;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true

app:
  security:
    jwt:
      secret: courtpulse-test-secret-key-with-32-byte-minimum-length
